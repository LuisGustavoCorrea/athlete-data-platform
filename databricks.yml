# This is a Databricks asset bundle definition for my_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: athlete
  uuid: e886cd9d-5d67-4e8e-8b39-d4e7d481e2c8

include:
  - resources/*.yml
  - resources/*/*.yml

sync:
  include:
    - src/notebooks/**

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-550989957113770.10.azuredatabricks.net
      root_path: /Workspace/Users/lgcpazdb892@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbw_athlete
      schema: dev
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-550989957113770.10.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/lgcpazdb892@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/lgcpazdb892@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbw_athlete
      schema: prod
    permissions:
      - user_name: lgcpazdb892@outlook.com
        level: CAN_MANAGE
